{% for coll in collections %}

  {% case coll.handle %}
    {% when 'frontpage' %}

      {% for prod in coll.products %}
        {{ prod.title }}
      {% endfor %}

    {% else %}

      no products
  {% endcase %}

{% endfor %}